import styled from 'styled-components';

export const CardList = styled.div`
    display: flex;

    button {
        border-radius: 1px;
        color: orange;
        background-color: gray;
    }
`;
